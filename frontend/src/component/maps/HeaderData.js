import { Autocomplete } from "@react-google-maps/api";
import { Typography ,Box,InputBase,Toolbar} from '@mui/material';
const HeaderData = ({ setType, setRatings, setCoordinates }) => {
  return (
    <Box display="flex">
      <Typography>
        
      </Typography>

    </Box>
  );
};

export default HeaderData;
